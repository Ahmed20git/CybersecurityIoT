"""EffectShield: deterministic runtime enforcement for a simulated smart home.

Package layout follows the module boundaries in ``docs/requirements.md``:

- ``domain``: shared typed records, the action schema and errors.
- ``simulator``: device state machines, the simulation clock and execution.
- ``gateway``: trusted observation and request issuance.
"""

__version__ = "0.1.0"
